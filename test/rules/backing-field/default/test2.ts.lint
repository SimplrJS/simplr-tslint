export class Foo {
    constructor(private _bar: string) {}

    public get barName(): string {
               ~~~~~~~              [Accessor name must be the same as a backing field. Expected name "bar".]
        return this._bar;
    }
}
